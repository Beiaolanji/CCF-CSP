#include <iostream>
#include <vector>
using namespace std;

int main() {
  float n;
  scanf("%f", &n);
  if (n <= 3500) {
    printf("%d", int(n));
    return 0;
  }
  n -= 3500;
  if (n <= 1455) {
    n = n / 0.97;
    n += 3500;
  } else if (n <= 4155) {
    n = (n - 1455) / 0.9;
    n += (3500 + 1500);
  } else if (n <= 7755) {
    n = (n - 4155) / 0.8;
    n += (3500 + 1500 + 3000);
  } else if (n <= 27255) {
    n = (n - 7755) / 0.75;
    n += (3500 + 1500 + 3000 + 4500);
  } else if (n <= 41255) {
    n = (n - 27255) / 0.7;
    n += (3500 + 1500 + 3000 + 4500 + 26000);
  } else if (n <= 57505) {
    n = (n - 41255) / 0.65;
    n += (3500 + 1500 + 3000 + 4500 + 26000 + 20000);
  } else {
    n = (n - 57505) / 0.55;
    n += (3500 + 1500 + 3000 + 4500 + 26000 + 20000 + 25000);
  }
  printf("%d", int(n));
  return 0;
}
